# Database Configuration
DB_URL=jdbc:postgresql://localhost:5432/cogni_db
DB_USER=admin
DB_PASSWORD=admin

# Application Mode
# Options: local (development, skips auth), prod (production, requires auth)
# Defaults to 'local' if not set.
APP_MODE=local

# Legacy/Unused (Configured in build.gradle.kts for tests, but not used in app logic)
DEV_MODE=false

# JWT Configuration (Optional - defaults shown below are used if missing)
JWT_SECRET=dev-secret-key
JWT_ISSUER=http://0.0.0.0:8080/
JWT_AUDIENCE=http://0.0.0.0:8080/hello
JWT_REALM=Access to 'hello'
